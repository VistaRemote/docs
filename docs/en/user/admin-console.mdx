# Admin Console (Users)

For **enterprise IT / administrators** managing users, devices, sessions, and compliance data.

As of **1.11.0**, the modules below use live server data, not demo data. **1.13.0** adds **CSV export** on orders, organizations, and recordings (users, devices, security events, and jobs already had it); **online / offline** device filter; audit **result** filter (`ok` / `denied` / `error`); security-event **deviceId** filter; and dashboard **org count** plus **pending orders**. **1.14.0** adds sessions / permissions-matrix **CSV**; user **org ID** filter; efficiency-report **userId / sessionId** filters; jobs **id** filter; and audit **resource type** filter. **1.15.0** adds user / security **userId** filters; device **owner / org** filters with overview deep-link for online devices; order **provider** filter; org-detail **members CSV**; System **copy ICE probe result**; recording **recordingId / deviceId** filters. **1.18.0** routes devices / recordings / organizations / sessions / jobs / orders list filters through **server query params**; overview **failed recordings** deep-links; efficiency reports add **deviceId** filter. **1.19.0** seeds most lists from **URL query params** (including plan expiry, org seats, audit result, session mode); overview deep-links expired users / over-seat orgs / denied audits / SFU sessions; System can **copy TURN URL**; efficiency reports add **recordingId** filter. **1.20.0** completes URL seeds for recordings / orders / sessions / organizations / jobs / security / reports; adds permission-matrix search; overview adds offline devices / available seats / expired recordings / paid orders deep-links. **1.21.0** seeds audit **fromTs / toTs** from the URL; System can copy **ICE summary / pairing-policy JSON**; permission search accepts `q` / `perm` URL seeds; org members filter by role; overview links to **efficiency reports**. **1.25.0** (Scope C): Agent **MediaRecorder** upload playback; **Permissions** can assign platform-operator roles and orgs can change member `orgRole` (matrix stays read-only); orders distinguish local_billing / commerce; security events deep-link to session/device; sessions support **observe**; runtime has a **2×2 monitor-wall** shell (full SFU multi-stream still later). **1.27.0**: org drawer manages **SupportGrant** (temporary IT-assist grants: create / list / revoke; permission matrix stays read-only); recording playback uses a **short-lived signed playback-url** (`expiresAt`, refresh on expiry; proxied via in-memory `/media`); Client **Membership** checkout picks WeChat / Alipay **catalog `providerHint`** (`wechat` / `alipay`, not Entitlement adapter ids). **1.28.0**: Enterprise device rows can toggle **allowOrgSupport** (POL-02 path A); Consumer devices show **—**; after Membership commerce **complete** / paid-status poll, local **User.plan** syncs so the Admin Users list shows Pro/Enterprise without waiting on a production PSP webhook. **1.29.0**: editable Security **alert rules lite** (FR-WAD-107): banner min severity + muted event types; not a full correlation engine; monitor-wall slot **in-wall observe** embeds single-stream observe (full SFU multi-stream still later); lite Entitlement webhook `POST /api/v1/commerce/webhooks/entitlement` (`x-lw-signature: v1=<base64url>`; HMAC `${timestamp}.${nonce}.${rawBody}`) syncs **User.plan** (not production WeChat/Alipay PSP). **1.30.0**: read-only entitlement/payment flags on the System page (`entitlementMode`, `baseUrlConfigured`, `webhookSecretConfigured`; no secrets); org **SupportGrant** revoke confirmation can check **Revoke and disconnect related sessions** (`?kickSessions=true`, off by default); Agent recording coalesces non-final multipart PUTs to **≥5 MiB** for S3/MinIO (`memory://` still allows small parts). **1.31.0**: security-events list **applies saved mute rules by default** (`applyMute=true`); Switch **Apply mute rules** (`applyMute=false` shows all types); overview deep-links to **/security**, **/system** (entitlement flags), and **/monitor-wall**. **1.32.0**: security events **`applyMute` URL seed** (`?applyMute=true|false`, synced with the Switch); audit action presets add **observe / SupportGrant / operator and member roles**. **1.33.0**: org drawer **SupportGrant** **CSV export**; System **copy entitlement status JSON** (flags only: `entitlementMode`, `entitlementBaseUrlConfigured`, `webhookSecretConfigured`); Recordings show **EntityTooSmall / part-size** hints when `failed` (banner + tooltips; **≥5 MiB** coalesce — Agent since 1.30). **1.35.0**: monitor-wall **layout persistence** (FR-WAD-108 layout slice) — URL `s0`–`s3` take priority; without seeds **GET** `/admin/monitor-wall/layout` restores bindings; **Save layout** **PUT** to the same path (requires **admin.write**); slot **in-wall observe** stays **FR-WAD-72** single-stream; cross-session **SFU multi-stream** **FR-WAD-108** still open. **1.36.0**: `POST /admin/rooms/:roomId/observe` returns shared **AdminObserveTicket** (`signalingTicket`, `role: observe`, …); SFU fields (`routerId` / `transport` / `consumer`) are **null stubs** — not mediasoup handshake yet; monitor-wall deep links from overview / sessions / security; audit **layout** preset. **1.37.0**: **Jobs** list filters (`id` / `name` / `state` / `failedReason`) **sync to the URL** (overview **failed jobs** → `/jobs?state=failed`); `recording.summarize` rows (`summarize-{recordingId}`) link **Recording** → `/recordings?recordingId=…`; **Efficiency reports** **Open recordings** carries active substring filters to `/recordings`; audit **`admin.monitor_wall.layout`** rows jump **Monitor wall** (`s0=roomId` when meta has one — layout seed only, not SFU multi-stream). **1.38.0**: user detail drawer **related links** (Sessions / Recordings / Orders / Devices / Audit with `userId` / `ownerId` / `actorId` filters); **Monitor wall** in Devices **Actions** when the device has an active session (`/monitor-wall?s0=<sessionId>` — slot seed only); Sessions list **`?userId=`** filter synced to the URL; layout-save audit meta includes **`roomIds`** (and first `roomId`) so audit layout rows can jump with **`?s0=`** — **not** SFU multi-stream shipped. **1.39.0** — read-only **Pairings** list at **`/pairings`** (**FR-WAD-109**): **GET** `/api/v1/admin/pairings` (**admin.read**); filters **`deviceId` / `userId` / `numericCode`** sync to the URL; row **Device** / **User** deep-links to `/devices?deviceId=` and `/users?userId=` (device owner when known). **1.40.0** — on **System**, the pairing-policy section **Open pairings** → `/pairings`; Pairings row **Session** → `/sessions?sessionId=…` when **sessionId** is present. **1.41.0** — Pairings **consumed** filter (`?consumed=yes|no`, server + URL-synced); **Overview** **Pairings** NavCard → `/pairings`; **Devices** **Actions** **Pairings** → `/pairings?deviceId=`. **1.42.0** — user detail drawer **Open pairings** → `/pairings?userId=`; **Membership** page **Open orders** → `/orders?userId=` (URL `?userId=` or the current commerce order **userId**). **1.43.0** — **Orders** row **Membership** → `/membership?userId=…` (completes **Membership**↔**Orders**); audit action presets add **`pairing.create`** / **`pairing.join`** (server audit since 1.12.0). **1.44.0** — **Forbidden** (**403**) adds **Back to dashboard** (→ `/`); signed-in users get **Sign in with another account**; **Pairings** header **Create audit** / **Join audit** → `/audit?action=pairing.create|join`; monitor-wall **info alert** clarifies **FR-WAD-108 layout persistence ✅** vs multi-stream **SFU still open**. Ops FAQ: [FAQ](/user/faq) (mute rules, SupportGrant, entitlement flags, recording parts, monitor wall, user drawer links, jobs/reports jumps, audit layout → monitor wall, pairings list, consumed filter, session deep-links, Users→Pairings, Membership↔Orders, pairing audit presets, Forbidden dashboard, Pairings→Audit header, monitor-wall layout vs SFU alert). Roles: [Authorization](/architecture/authorization).

**1.52.0 current viewer Admin (honesty: do not accept unrouted pages)**

Current `apps/admin` routes: **Overview, Users, Devices, Orders, Sessions, Recordings, Permissions, Audit, System**. Modules that may exist in historical Admin / API but are **not mounted in the current viewer UI**: Membership, Pairings, Organizations, Jobs, efficiency reports, monitor wall, security events.

## Access

- URL is typically `https://<your-domain>/admin` (provided by your operator)
- Sign in with a **platform admin** or **org admin** account

## Modules

| Module | Purpose |
| :--- | :--- |
| **Overview** | Live count cards, including **org count** and **pending orders**, plus users, sessions, recordings, devices / online devices, and similar. **/system** is mounted (entitlement flags). Historical deep-links to **/security**, **/monitor-wall**, and **Pairings** NavCard → `/pairings` (**1.41.0**) may **404** in the current viewer (see 1.52 honesty note above) |
| **Users** | View / filter (including **user ID** and **org ID**) / create / disable accounts; assign org and plan; detail drawer **related links** (Sessions / Recordings / Pairings / Orders / Devices / Audit, **1.38.0**; **1.42.0** adds **Pairings**); export **CSV** |
| **Devices** | Agent **online** status (heartbeat within about 120 seconds), **online / offline**, **owner / org**, and **label** filters, label edit, revoke device; **Pairings** in **Actions** → `/pairings?deviceId=` (**1.41.0**); **Monitor wall** in **Actions** when the device has an active session (`s0` seed, **1.38.0**); export **CSV** (overview **Online devices** deep-links to the online filter) |
| **Pairings** | Read-only recent pairing codes from PostgreSQL (**FR-WAD-109**, **1.39.0**); filter by **device ID**, **owner user ID**, **numeric code**, and **consumed** (`yes` = `consumedAt` set, `no` = unused — URL-synced, **1.41.0**); row **Device** / **User** / **Session** links to `/devices?deviceId=`, `/users?userId=`, `/sessions?sessionId=` (**1.40.0** session deep-link); page header **Create audit** / **Join audit** → `/audit?action=pairing.create|join` (**1.44.0**); pairing **policy** (length / TTL) on **System**, with **Open pairings** → `/pairings` in the policy section |
| **Sessions** | Active remote rooms (p2p / SFU); admins can **force disconnect** / **observe** (single stream; **1.36.0** observe ticket = shared **AdminObserveTicket**, SFU stub fields null); filter by **user ID** (**`?userId=`** URL-synced, **1.38.0**); export **CSV** |
| **Recordings** | **Filter** by user, **recording ID**, **device ID**, status, session, and trigger; export **CSV**; **EntityTooSmall / part-size** hints on `failed` rows (banner + tooltips; **≥5 MiB** coalesce); built-in player fetches a **playback-url** short-lived signed stream (`expiresAt`, refresh on expiry; in-memory `/media`); **seek** and **playback speed** |
| **Audit** | **Filter** by actor, action presets (including **observe / SupportGrant / operator and member roles**, **`pairing.create` / `pairing.join`** (**1.43.0**), and **monitor-wall layout**), time, resource / **resource type**, and **result** (`ok` / `denied` / `error`); **`admin.monitor_wall.layout`** rows link **Monitor wall** (meta **`roomIds`** / `roomId` → `?s0=`, slot seed only, **1.38.0**); export **CSV** |
| **Security events** | Exfiltration risk, bulk delete, behavior inference, and similar; filter by type, **deviceId**, **userId**, and **orgId**; editable **alert rules lite** (banner min severity + muted types; not a correlation engine); list **applies saved mute rules by default** (Switch **Apply mute rules**; `applyMute=false` or URL `?applyMute=false` shows all types); high-severity banner; jump to a related recording timestamp / **session / device**; export **CSV** |
| **Organizations** | Orgs, seats/members, and policy (keywords, domains, and similar); change member **orgRole**; drawer **SupportGrant** (IT-assist) create / list / revoke / **export CSV** (optional **Revoke and disconnect related sessions**, `?kickSessions=true`); list **CSV**; detail **members CSV** |
| **Orders** | Filter plan orders by user, SKU, **provider**, **source**, and status. The list can show local billing and commerce mirror rows. **Simulate paid** appears only on `source=local_billing` pending rows and calls `POST /api/v1/admin/orders/:id/simulate-paid`. Commerce rows have no button; the API returns **400** `COMMERCE_SIMULATE_PAID_FORBIDDEN` (use Client Membership complete / webhook). Production without `BILLING_ALLOW_SIMULATE_PAID` returns **400** `SIMULATE_PAID_DISABLED`. Do **not** use an Admin JWT against `/billing/.../simulate-paid`. Page copy: WeChat / Alipay production notify is on Entitlement, not this page. Historical row **Membership** → `/membership?userId=…` has **no Admin Membership route** in the current viewer |
| **Membership** | **Checkout is Web Client `/membership`** (current viewer Admin has **no such route**): trial countdown, legal, SKU, catalog `providerHint` (`wechat` / `alipay`), QR / `codeUrl` / redirect; after pay, **status polling** + **complete** refreshes entitlements. On-page sandbox copy is **not** production PSP. Historical Admin Membership (`?userId=`, Open orders) is unrouted |
| **System** | **FR-WAD-52** entitlement/payment config flags (read-only): `entitlementMode`, `entitlementBaseUrlConfigured`, `webhookSecretConfigured`; **Copy entitlement status JSON**. Never exposes secrets or Base URL plaintext. Enforce readiness ≠ live WeChat/Alipay merchant |
| **Jobs** | Background jobs (for example recording summaries): list; filter by **id / name / state / failedReason** (URL-synced; overview **failed jobs** → `?state=failed`); **`recording.summarize`** rows link to **Recordings**; retry failed jobs; export **CSV** |
| **Efficiency reports** | Weekly / monthly recording-minute totals; filter by **userId / sessionId / deviceId / recordingId** before rollup (from recording metadata, not an LLM efficiency model); **Open recordings** deep-link with active filters; export **CSV** |
| **Permissions** | Platform / org permission matrices are **read-only**; export reference **CSV**; assign **platform operator** roles (not checkbox Permission edits) |
| **Monitor wall** | **Enterprise multi-screen wall**: thumbnail observe, click-up to HD control; layout can be saved. See [Device Fleet & Enterprise](/user/fleet-and-enterprise) and [Monitor wall layout](#monitor-wall-layout) |

## Common tasks

### Grant Pro to a user

1. Closed trial preferred: Admin **Users** → change plan (writes local `User.plan` immediately).
2. Sandbox checkout: the user opens Web **Client** `/membership`, picks `providerHint` (WeChat / Alipay), then waits for polling or clicks **Complete payment**. That is not live merchant notify.
3. Local pending orders only: Admin **Orders** **Simulate paid** (`POST /api/v1/admin/orders/:id/simulate-paid`). The order owner may also call `/api/v1/billing/orders/:id/simulate-paid` with a Controller JWT. Commerce orders return `COMMERCE_SIMULATE_PAID_FORBIDDEN`.
4. Entitlements apply after the user refreshes the client or signs in again.

### Disable a user

1. Users → select user → disable.
2. The user cannot start new sessions. For an active session, open **Sessions** and **force disconnect**.

### Monitor wall layout

**FR-WAD-108 layout slice (1.35.0)** — persists slot-to-session bindings; **does not** include cross-session SFU multi-stream live subscription (that part of **FR-WAD-108** is still open; do not accept it as shipped).

1. Open **/monitor-wall**. URL query **`s0`–`s3`** (aliases `slot0`–`slot3` accepted) with session IDs **take priority** and are **not** overwritten by a saved layout.
2. With **no** URL seeds, the page **GET**s `/admin/monitor-wall/layout` to restore the last saved slot bindings.
3. After changing slots, click **Save layout** to **PUT** the same path (requires **admin.write**; read-only admins see the button disabled). Saving the layout **does not** auto-start in-wall observe or SFU connections.
4. Per-slot **in-wall observe** remains **FR-WAD-72 MVP** single-stream embed; the full cross-session **SFU multi-stream** monitor wall is **FR-WAD-108** (Spec drafted; **not delivered**; depends on SFU multi-producer + MD-4).
5. **1.36.0** — `POST /admin/rooms/:roomId/observe` returns shared **AdminObserveTicket** (`signalingTicket`, `role: observe`, `expiresAt`, …). SFU fields (`routerId`, `transport`, `consumer`) are **null stubs** today — not mediasoup-client handshake DTOs; do not treat as multi-stream SFU shipped.
6. **1.37.0** / **1.38.0** — Filter audit by action preset **monitor-wall layout** (`admin.monitor_wall.layout`). **1.38.0** — **Save layout** writes audit meta with **`roomIds`** (bound session ids) and the first **`roomId`**. Matching rows show **Monitor wall** in **Related** → `/monitor-wall`, or `/monitor-wall?s0=<roomId>` from meta / resource / target `roomId` / `room_id`. Slot 0 seed only — does **not** start SFU multi-stream or in-wall observe.
7. **1.44.0** — The page **info alert** states that **FR-WAD-108 layout persistence** is shipped ✅ while **multi-stream SFU subscription per slot** is still open; **FR-WAD-72 MVP** (2×2 grid + in-wall single-stream observe) remains available.

### Jobs, reports, and recordings (1.37.0)

1. **Jobs** — List filters (`id`, `name`, `state`, `failedReason`) **sync to the URL** when you search (shareable links; overview **failed jobs** opens `/jobs?state=failed`).
2. **Recording summarize jobs** — Rows with type `recording.summarize` and id `summarize-{recordingId}` show **Recording** in **Actions**, linking to `/recordings?recordingId=…` (other job types have no recording jump).
3. **Efficiency reports** — **Open recordings** opens `/recordings` with the same active substring filters (`userId`, `sessionId`, `deviceId`, `recordingId`); empty filters open the full list.

### Pairings list (1.39.0 / 1.40.0 / 1.41.0)

**FR-WAD-109** — read-only ops view of recent pairing codes; **not** a code generator or SFU / monitor-wall feature.

1. Open **/pairings** under **Operations** (nav between **Devices** and **Orders**), from **Overview** → **Pairings** NavCard (**1.41.0**), from **Devices** → **Actions** → **Pairings** (`?deviceId=` pre-filled, **1.41.0**), or from **System** → pairing-policy section **Open pairings** (**1.40.0**).
2. The page calls **GET** `/api/v1/admin/pairings` (requires **admin.read**). Default returns up to **100** recent rows; optional query **`limit`** is clamped **1–500**.
3. Server filters: **`deviceId`** and **`numericCode`** (substring, case-insensitive); **`userId`** filters by the device **owner** resolved from the devices table; **`consumed`** (**1.41.0**) — **`yes`** = `consumedAt` set (code already used), **`no`** = `consumedAt` null (unused); unknown values are ignored.
4. Search fields sync to **`?deviceId=`**, **`?userId=`**, **`?numericCode=`**, and **`?consumed=yes|no`** in the URL (shareable links). The **Consumed** select offers **Consumed** / **Unused** / **All**.
5. Columns include numeric code, **deviceId**, **userId** (owner), **sessionId**, **expiresAt**, **consumedAt**. In **Actions**, **Device** → `/devices?deviceId=…`; **User** → `/users?userId=…` when an owner is known; **Session** → `/sessions?sessionId=…` (**1.40.0**, when the row has **sessionId**; Sessions filter syncs to **`?sessionId=`** in the URL).
6. To change code length or TTL, use **System** pairing policy; hosts still generate codes on the Agent.
7. **1.44.0** — Page header **Create audit** / **Join audit** open `/audit?action=pairing.create` or `pairing.join` (same presets as the Audit action filter since **1.43.0**).

### User drawer and cross-module links (1.38.0 / 1.42.0)

1. **Users** — In the detail drawer, **View sessions / recordings / pairings / orders / devices / audit** deep-link to `/sessions?userId=…`, `/recordings?userId=…`, `/pairings?userId=…` (**1.42.0**; Pairings list filters by device **owner** substring), `/orders?userId=…`, `/devices?ownerId=…`, and `/audit?actorId=…` (devices list filters by `ownerId`; the drawer device tab still uses owner / org logic).
2. **Devices** — When a row has an **active session** (correlated with in-memory rooms), **Actions** shows **Monitor wall** → `/monitor-wall?s0=<sessionId>` (slot 0 seed; **not** SFU multi-stream shipped).
3. **Sessions** — Filter by **user ID**; filters sync to **`?userId=`** in the URL (the user drawer **View sessions** button seeds this param). Client-side filter matches row `userId` (rows without `userId` do not match).

### Membership and orders (1.42.0 / 1.43.0 / 1.52.0)

1. **Checkout** — Web **Client** `/membership` (current viewer Admin has **no Membership route**): `providerHint`, QR/redirect, status polling after pay, and **Complete payment** (`complete`). Sandbox copy is not production PSP.
2. **Orders** — Admin **Orders** lists local rows; **Simulate paid** is `local_billing` pending only. Read commerce together with Client Membership. Historical Admin Membership↔Orders deep-links are **unrouted** in the current viewer.
3. **Grant** — If a trial user is stuck on Free, prefer **Users** → change plan; do not simulate-pay commerce orders.

## Not in this release

Do not treat the following as delivered:

- An **editable permission matrix** (the Permissions page is a read-only reference)
- A full cross-session multi-stream **SFU monitor wall** (**FR-WAD-108**: Spec drafted; implementation open; depends on SFU multi-producer + MD-4; current product is **FR-WAD-72 MVP**: 2×2 shell + in-wall single-stream observe)
- A **correlation engine** or alert **DSL** (alert rules lite is banner threshold + mute types only)
- Production **WeChat Pay / Alipay** secrets (never commit them; sandbox lives on Entitlement)

Remaining gaps: [Implementation status](/engineering/implementation-status).

## Security

- Prefer VPN or a private network for the admin URL; MFA is planned (P1).
- Audit sensitive actions (disable user, plan changes, force disconnect, device revoke); filter and export CSV as needed.

## Ops FAQ

Mute rules, SupportGrant, entitlement flags, recording EntityTooSmall, monitor-wall vs full SFU wall, user drawer links, jobs/reports recording jumps, audit layout → monitor wall, pairings list, consumed filter, Overview/Devices entry points, session deep-links, Users→Pairings, Membership↔Orders, pairing audit presets, Forbidden dashboard, Pairings→Audit header, and monitor-wall layout vs SFU alert: [FAQ](/user/faq) § Admin & remote ops.

Developers: [Admin getting started](/guide/admin-getting-started).

## Payments and Entitlement (mock → sandbox)

| Mode | Behavior |
| :--- | :--- |
| `ENTITLEMENT_MODE=off` (default) | Local plan/orders; no central Entitlement |
| `ENTITLEMENT_MODE=enforce` | Entitlement is authoritative for commercial checkout |
| Mock / dev | Owner `POST /api/v1/billing/orders/:id/simulate-paid`; Admin `POST /api/v1/admin/orders/:id/simulate-paid` (`local_billing` only) |
| Sandbox WeChat / Alipay | Client `/membership`: `providerHint` `wechat`→`wechat_pay_v3`, `alipay`→`alipay_f2f`; poll status + `complete`; secrets only in Entitlement |
| Lite Entitlement webhook | `POST /api/v1/commerce/webhooks/entitlement`; `x-lw-timestamp` / `x-lw-nonce` / `x-lw-signature: v1=<base64url>` / `x-lw-event-id`; HMAC `${timestamp}.${nonce}.${rawBody}`; syncs **User.plan** (not production WeChat/Alipay PSP) |

Production checklist (entitlement **API** flags, bad-signature 401, commerce simulate-paid forbidden, env **names**): [Production payment readiness](/user/payment-production). Real merchant onboarding is owner-ops, not shipped.
