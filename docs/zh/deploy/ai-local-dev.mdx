# 本地 AI 开发环境（私有化栈）

用于调试 **行为分析 + 自托管 LLM + 向量库 + Python ML** 全链路，**无需公网 API Key**。

需 **core + dev** 两层；AI 服务在 dev 层 **`ai`** profile 中按需启用。

## 前置

- Docker Desktop
- Node.js 24+、pnpm 9+
- Python 3.11+（仅当在宿主机直接跑 `python-worker` 时）

## 启动基础设施

```bash
cd deploy
cp compose/.env.example .env

# 基线（postgres、redis、server）
docker compose -f compose/docker-compose.core.yml -f compose/docker-compose.dev.yml up -d

# Ollama + Qdrant + python-worker
docker compose -f compose/docker-compose.core.yml -f compose/docker-compose.dev.yml --profile ai up -d
```

| 服务 | 地址 |
| :--- | :--- |
| PostgreSQL | `localhost:5637` |
| Redis | `localhost:6380` |
| Ollama（LLM） | `http://localhost:11435` |
| Qdrant（向量） | `http://localhost:6335` |
| python-worker | `http://localhost:4100` |

宿主机 **11435** / **6335** 与 VistaCast 默认端口（`11434` / `6333`）错开。

拉取模型：

```bash
docker compose -f compose/docker-compose.core.yml -f compose/docker-compose.dev.yml exec ollama ollama pull llama3.2
# 可选嵌入模型（RAG）
docker compose -f compose/docker-compose.core.yml -f compose/docker-compose.dev.yml exec ollama ollama pull nomic-embed-text
```

## 启动应用

```bash
cd shared && pnpm install && pnpm build
cd server && cp .env.example .env && pnpm install && pnpm start:dev
cd ai && cp .env.example .env && pnpm install && pnpm start:dev
```

`ai/.env` 要点：

```ini
REDIS_URL=redis://localhost:6380
AI_BASE_URL=http://localhost:11435/v1
AI_MODEL=llama3.2
VECTOR_DB_URL=http://localhost:6335
PYTHON_ML_URL=http://localhost:4100
```

## 架构说明

- [AI 平台](/architecture/ai-platform) — 私有化卖点、Node vs Python
- [AI 行为洞察](/architecture/ai-behavior) — 端侧监控与降级

Compose 细节：MetaRepo `deploy/README.md`；规格：`spec/ai-platform-spec.md`。
